# نظام العلاقات اللغوية المتكيفة
# Adaptive Linguistic Relations System

## 📋 نظرة عامة | Overview

**نظام العلاقات اللغوية المتكيفة** هو نظام ذكي متقدم لاكتشاف واستخراج واستنتاج العلاقات بين الكلمات في اللغة العربية. يعتمد النظام على **النظريات الثلاث الثورية** (ثنائية الصفر، تعامد الأضداد، الفتائل) ويتكامل مع **المعادلة الأم** و**لغة البيان**.

The **Adaptive Linguistic Relations System** is an advanced intelligent system for discovering, extracting, and inferring relationships between words in Arabic. The system is based on the **three revolutionary theories** (Zero Duality, Perpendicular Opposites, Filaments) and integrates with the **Mother Equation** and **Bayan Language**.

---

## 🎯 الأهداف | Objectives

### الأهداف الرئيسية | Main Objectives

1. **اكتشاف العلاقات التلقائي** - Automatic relation discovery
   - استخراج العلاقات من النصوص العربية
   - التعرف على 20+ نوع من العلاقات اللغوية
   - حساب الثقة والتكرار لكل علاقة

2. **الاستنتاج الذكي** - Intelligent inference
   - استنتاج علاقات جديدة من العلاقات الموجودة
   - الاستنتاج المنطقي باستخدام قواعد المنطق
   - الاستنتاج بالقياس (Analogy-based inference)

3. **التكيف المستمر** - Continuous adaptation
   - تحديث قوة العلاقات مع الاستخدام
   - تطبيق النظريات الثلاث للتكيف
   - بناء شبكة دلالية متطورة

4. **التكامل مع الأنظمة الأخرى** - Integration with other systems
   - التكامل مع قاعدة المعرفة (IdeaKnowledgeBase)
   - التحقق المنطقي من العلاقات (LogicalVerifier)
   - استخدام التطبيع اللغوي (LinguisticNormalizer)

---

## 🏗️ البنية المعمارية | Architecture

### المكونات الرئيسية | Main Components

```
نظام العلاقات اللغوية المتكيفة
├── 📚 قاعدة النصوص (TextCorpus)
│   ├── تخزين النصوص
│   ├── فهرسة حسب الفئة
│   ├── فهرسة حسب الكلمات المفتاحية
│   └── البحث والاستعلام
│
├── 🧩 محرك استخراج العلاقات (RelationExtractor)
│   ├── تحليل الأنماط اللغوية
│   ├── استخراج العلاقات
│   ├── حساب الثقة
│   └── تجميع ودمج العلاقات
│
├── 🕸️ الشبكة الدلالية (SemanticNetwork)
│   ├── العقد (الكلمات)
│   ├── الحواف (العلاقات)
│   ├── تطبيق النظريات الثلاث
│   └── البحث عن العلاقات المباشرة وغير المباشرة
│
├── 🔍 محرك الاستعلام والاستنتاج (RelationQueryEngine)
│   ├── الاستعلام عن العلاقات
│   ├── الاستنتاج المنطقي
│   ├── الاستنتاج بالقياس
│   └── توليد الجمل
│
└── 🎯 النظام المتكامل (AdaptiveLinguisticRelations)
    ├── تنسيق جميع المكونات
    ├── التكامل مع الأنظمة الأخرى
    └── واجهة موحدة للاستخدام
```

---

## 📊 أنواع العلاقات | Relation Types

النظام يدعم 20+ نوع من العلاقات اللغوية:

### 1. علاقات الهوية | Identity Relations
- `IS_A` - هو/هي
- `INSTANCE_OF` - مثال على
- `TYPE_OF` - نوع من

### 2. علاقات الملكية | Possession Relations
- `HAS_A` - لديه/لديها
- `PART_OF` - جزء من
- `CONTAINS` - يحتوي على

### 3. علاقات الفعل | Action Relations
- `DOES` - يفعل
- `ACTS_ON` - يؤثر على
- `CREATES` - ينتج/يصنع
- `USES` - يستخدم

### 4. علاقات المكان | Location Relations
- `LOCATED_IN` - يقع في
- `GOES_TO` - يذهب إلى
- `COMES_FROM` - يأتي من
- `LIVES_IN` - يعيش في

### 5. علاقات الزمن | Time Relations
- `HAPPENS_AT` - يحدث في
- `BEFORE` - قبل
- `AFTER` - بعد
- `DURING` - أثناء

### 6. علاقات السببية | Causality Relations
- `PURPOSE` - الغرض
- `REASON` - السبب
- `RESULT` - النتيجة

### 7. علاقات الصفات | Attribute Relations
- `HAS_PROPERTY` - له خاصية
- `CHARACTERIZED_BY` - يتميز بـ

### 8. علاقات اجتماعية | Social Relations
- `LOVES` - يحب
- `HELPS` - يساعد
- `TEACHES` - يعلم
- `LEARNS_FROM` - يتعلم من

### 9. علاقات عامة | General Relations
- `RELATED_TO` - مرتبط بـ

---

## 🔬 النظريات الثلاث | Three Theories

### 1. ثنائية الصفر | Zero Duality

```typescript
D_لغوي = (P_لغوي, N_لغوي, B_لغوي(t))
B_لغوي(t) = P_لغوي(t) - N_لغوي(t)
where: -1 ≤ B(t) ≤ 1
```

**التطبيق في العلاقات:**
- كل كلمة لها توازن ثنائي (dualityBalance)
- الكلمات المتضادة لها توازنات معاكسة
- التوازن يؤثر على قوة العلاقات

### 2. تعامد الأضداد | Perpendicular Opposites

```typescript
∀v⃗_دلالي ∈ ℝⁿ, ∃v⃗⊥_دلالي : v⃗_دلالي · v⃗⊥_دلالي = 0
```

**التطبيق في العلاقات:**
- كل كلمة لها متجه دلالي (semanticVector)
- المتجهات المتعامدة تمثل معاني مختلفة تماماً
- يستخدم في حساب القرب الدلالي

### 3. نظرية الفتائل | Filament Theory

```typescript
f_لغوي(x) = Σᵢ fᵢ_لغوي(x)
```

**التطبيق في العلاقات:**
- العلاقات المعقدة تتكون من فتائل بسيطة
- كل فتيل يمثل علاقة أساسية
- التركيب يولد علاقات جديدة

---

## 💻 الاستخدام | Usage

### مثال أساسي | Basic Example

```typescript
import { AdaptiveLinguisticRelations } from './adaptiveLinguisticRelations';

// إنشاء النظام
const system = new AdaptiveLinguisticRelations();

// إضافة نصوص
system.addTexts([
  { text: 'الولد يذهب إلى المدرسة', category: 'تعليم' },
  { text: 'التلميذ يدرس في المدرسة', category: 'تعليم' },
  { text: 'المعلم يعلم التلاميذ', category: 'تعليم' }
]);

// اكتشاف العلاقات
const discovery = system.discoverRelations(['ولد', 'مدرسة']);
console.log(`استخرجت ${discovery.extracted.length} علاقة`);

// استعلام عن علاقات
const query = system.queryRelations('ولد', 'مدرسة');
console.log(`علاقات مباشرة: ${query.directRelations.length}`);
console.log(`علاقات غير مباشرة: ${query.indirectRelations.length}`);

// توليد جمل
const sentences = system.generateSentences('ولد', 'مدرسة');
sentences.forEach(s => console.log(`• ${s}`));
```

### مثال متقدم | Advanced Example

```typescript
// التكامل مع قاعدة المعرفة
system.integrateWithKnowledgeBase();

// التحقق المنطقي
const verification = system.verifyRelationsLogically();
console.log(`✅ ${verification.verified} علاقة صحيحة`);
console.log(`⚠️  ${verification.needsReview} علاقة تحتاج مراجعة`);

// عرض الإحصائيات
system.displaySummary();
```

---

## 📈 الإحصائيات | Statistics

النظام يوفر إحصائيات شاملة:

### إحصائيات قاعدة النصوص
- إجمالي النصوص
- عدد الفئات
- عدد الكلمات المفتاحية
- متوسط طول النص

### إحصائيات الشبكة الدلالية
- عدد العقد (الكلمات)
- عدد الحواف (العلاقات)
- متوسط تكرار الكلمة
- متوسط قوة العلاقة
- متوسط معامل التكيف

### إحصائيات العلاقات
- إجمالي العلاقات المستخرجة
- إجمالي العلاقات المستنتجة
- متوسط الثقة
- توزيع أنواع العلاقات

---

## 🧪 الاختبارات | Tests

النظام يحتوي على **39 اختبار شامل**:

### اختبارات RelationExtractor (21 اختبار)
- ✅ استخراج العلاقات الأساسية
- ✅ التعامل مع "ال" التعريف
- ✅ حساب الثقة
- ✅ استخراج من نصوص متعددة
- ✅ تجميع ودمج العلاقات
- ✅ أنماط لغوية متقدمة
- ✅ حالات خاصة
- ✅ حفظ السياق
- ✅ علاقات متعددة في نفس الجملة

### اختبارات SemanticNetwork (18 اختبار)
- ✅ إضافة العلاقات
- ✅ البحث عن العلاقات
- ✅ العلاقات غير المباشرة
- ✅ إدارة العقد
- ✅ آليات التكيف
- ✅ الإحصائيات
- ✅ الحصول على البيانات

**النتيجة: 39/39 اختبار ناجح** ✅

---

## 🔗 التكامل | Integration

### التكامل مع قاعدة المعرفة

```typescript
// دمج الأفكار من قاعدة المعرفة
system.integrateWithKnowledgeBase();
```

يقوم النظام بـ:
1. استخراج جميع الأفكار من قاعدة المعرفة
2. تحويل كل فكرة إلى نص
3. إضافة النصوص إلى قاعدة النصوص
4. استخراج العلاقات من الأفكار

### التحقق المنطقي

```typescript
// التحقق من صحة العلاقات منطقياً
const verification = system.verifyRelationsLogically();
```

يقوم النظام بـ:
1. استخراج جميع العلاقات من الشبكة
2. التحقق من كل علاقة باستخدام LogicalVerifier
3. تصنيف العلاقات (صحيحة / تحتاج مراجعة)
4. إرجاع تقرير شامل

---

## 🎓 أمثلة عملية | Practical Examples

### مثال 1: اكتشاف علاقات بين "ولد" و "مدرسة"

```typescript
const discovery = system.discoverRelations(['ولد', 'مدرسة']);

// النتائج:
// ✅ ولد ← يذهب_إلى → مدرسة (ثقة: 0.90)
// ✅ ولد ← يقع_في → مدرسة (ثقة: 1.00)
```

### مثال 2: استعلام تفصيلي

```typescript
const query = system.queryRelations('تلميذ', 'كتاب');

console.log(`قوة العلاقة: ${query.relationStrength}`);
console.log(`القرب الدلالي: ${query.semanticProximity}`);
console.log(`أنواع العلاقات: ${query.relationTypes.join(', ')}`);
```

### مثال 3: توليد جمل

```typescript
const sentences = system.generateSentences('معلم', 'تلاميذ');

// النتائج:
// • المعلم يعلم التلاميذ
// • المعلم يشرح الدرس للتلاميذ
```

---

## 📚 الملفات | Files

### الملفات الرئيسية

1. **`textCorpus.ts`** - قاعدة بيانات النصوص
2. **`relationExtractor.ts`** - محرك استخراج العلاقات
3. **`semanticNetwork.ts`** - الشبكة الدلالية المتكيفة
4. **`relationQueryEngine.ts`** - محرك الاستعلام والاستنتاج
5. **`adaptiveLinguisticRelations.ts`** - النظام المتكامل

### ملفات الاختبارات

1. **`relationExtractor.test.ts`** - 21 اختبار
2. **`semanticNetwork.test.ts`** - 18 اختبار

### ملفات الأمثلة

1. **`adaptiveLinguisticRelationsExample.ts`** - مثال شامل

---

## 🚀 المستقبل | Future

### التحسينات المخططة

1. **توسيع أنواع العلاقات**
   - إضافة علاقات عاطفية
   - إضافة علاقات ثقافية
   - إضافة علاقات تاريخية

2. **تحسين الاستنتاج**
   - استنتاج متعدد المستويات
   - استنتاج بالتشابه الدلالي
   - استنتاج بالسياق

3. **التكامل مع أنظمة أخرى**
   - التكامل مع نظام التوليد اللغوي
   - التكامل مع نظام الفهم اللغوي
   - التكامل مع نظام الترجمة

4. **تحسين الأداء**
   - تحسين خوارزميات البحث
   - استخدام الفهرسة المتقدمة
   - التخزين المؤقت الذكي

---

## 👨‍💻 المطور | Developer

**Basel Yahya Abdullah (Al-Mubtakir)**
- 40 عاماً من البحث اللغوي
- مبتكر لغة البيان
- مبتكر النظريات الثلاث الثورية

---

## 📄 الترخيص | License

هذا النظام جزء من **لغة البيان** وخاضع لنفس الترخيص.

---

**تم التوثيق بتاريخ:** 2025-11-03
**الإصدار:** 1.0.0
**الحالة:** ✅ مكتمل وجاهز للاستخدام

