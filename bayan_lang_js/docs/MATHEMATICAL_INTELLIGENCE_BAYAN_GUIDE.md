# 🎯 دليل أسس الذكاء الرياضي في لغة البيان
# Mathematical Intelligence Foundations in Bayan Language Guide

## 📚 نظرة عامة - Overview

تم دمج **أسس الذكاء الرياضي** بالكامل في لغة البيان، مما يتيح للمبرمج استخدام المعادلات المتكيفة ودالة سيغمويد المعممة ومعادلة الشكل العام بسهولة تامة.

The **Mathematical Intelligence Foundations** have been fully integrated into the Bayan language, allowing programmers to easily use adaptive equations, generalized sigmoid functions, and general shape equations.

---

## 🎨 المكونات الأساسية - Core Components

### 1. دالة سيغمويد المعممة - Generalized Sigmoid

**المعادلة الرياضية:**
```
σₙ(x; k, x₀) = α / (1 + e^(-k(x - x₀)^n))
```

**المعاملات:**
- `α` (alpha): السعة - Amplitude
- `n`: معامل التقطيع - Segmentation parameter
- `k`: معامل الحدة - Sharpness parameter
- `x₀`: نقطة المركز - Center point

**الاستخدام في البيان:**

```javascript
// سيغمويد تقليدية (n=1)
دع سيغمويد = أنشئ_سيغمويد(1, 1, 1, 0);
اطبع(سيغمويد.evaluate(0));  // 0.5

// سيغمويد حادة (n=7) - للانتقالات الحادة
دع حادة = أنشئ_سيغمويد(1, 7, 100, 0);

// منحنى جرسي (n=2) - للتوزيعات
دع جرس = أنشئ_سيغمويد(1, 2, 10, 0);
```

---

### 2. المكون الخطي - Linear Component

**المعادلة الرياضية:**
```
L(x; β, γ) = βx + γ
```

**المعاملات:**
- `β` (beta): الميل - Slope
- `γ` (gamma): نقطة التقاطع مع المحور y - Y-intercept

**الاستخدام في البيان:**

```javascript
// خط مستقيم بميل 2 ونقطة تقاطع 3
دع خط = أنشئ_خط_مستقيم(2, 3);
اطبع(خط.evaluate(0));   // 3
اطبع(خط.evaluate(5));   // 13
اطبع(خط.evaluate(10));  // 23
```

---

### 3. معادلة الشكل العام - General Shape Equation

**المعادلة الرياضية:**
```
f̂(x) = Σᵢ [αᵢ·σₙᵢ(x; kᵢ, x₀ᵢ)] + L(x; β, γ)
```

هذه المعادلة تجمع عدة حدود سيغمويد مع مكون خطي شامل.

**الاستخدام في البيان:**

```javascript
// معادلة بسيطة (خطي فقط)
دع معادلة = أنشئ_معادلة_شكل();
معادلة.setGlobalLinear({ beta: 0.5, gamma: 0 });

// معادلة مركبة (خطي + سيغمويد)
دع مركبة = أنشئ_معادلة_شكل();
مركبة.setGlobalLinear({ beta: 0.5, gamma: 0 });
مركبة.addSigmoidTerm({
  alpha: 2,
  n: 1,
  k: 1,
  x0: 5
});
```

---

## 🎨 الخصائص البصرية - Visual Properties

يمكنك إضافة خصائص بصرية لكل حد في المعادلة:

```javascript
دع شكل = أنشئ_معادلة_شكل();

شكل.addSigmoidTerm({
  alpha: 2,
  n: 3,
  k: 50,
  x0: 2
}, {
  lineColorStart: '#228B22',  // أخضر غامق
  lineColorEnd: '#32CD32',    // أخضر فاتح
  lineWidthStart: 2,          // سمك البداية
  lineWidthEnd: 1,            // سمك النهاية
  fillColorStart: '#90EE90',  // لون التعبئة عند البداية
  fillColorEnd: '#98FB98',    // لون التعبئة عند النهاية
  opacity: 0.8                // الشفافية
});
```

---

## 🛠️ الدوال المساعدة - Helper Functions

### دالة خطوة - Step Function

```javascript
دع خطوة = أنشئ_دالة_خطوة(5, 1);
اطبع(خطوة.evaluate(4));  // ~0
اطبع(خطوة.evaluate(5));  // ~0.5
اطبع(خطوة.evaluate(6));  // ~1
```

### منحنى جرسي - Bell Curve

```javascript
دع جرس = أنشئ_منحنى_جرسي(0, 2, 1);
اطبع(جرس.evaluate(-2));  // ~0
اطبع(جرس.evaluate(0));   // ~0.5
اطبع(جرس.evaluate(2));   // ~1
```

### موجة مربعة - Square Wave

```javascript
دع موجة = أنشئ_موجة_مربعة(4, 1, 2);
// موجة مربعة بدورة 4 وارتفاع 1 ودورتين
```

---

## 👁️ محرك العين المستنبطة - Inference Engine

يمكنك استنباط المعادلات من البيانات تلقائياً:

```javascript
// بيانات خط مستقيم
دع بيانات = [
  { x: 0, y: 1 },
  { x: 1, y: 3 },
  { x: 2, y: 5 },
  { x: 3, y: 7 }
];

// استنباط المعادلة
دع معادلة = استنبط_معادلة(بيانات);

// استخدام المعادلة للتنبؤ
اطبع(معادلة.evaluate(4));  // ~9 (تنبؤ)
```

---

## 🎨 محرك الرسام - Drawing Engine

### رسم معادلة

```javascript
دع خط = أنشئ_خط_مستقيم(2, 3);
دع رسم = ارسم_معادلة(خط, {
  xRange: { min: 0, max: 10 },
  resolution: 100,
  enableGradients: true,
  enableLineWidthGradients: true
});

اطبع("عدد النقاط: " + رسم.metadata.pointCount);
```

### تصدير إلى SVG

```javascript
دع جرس = أنشئ_منحنى_جرسي(0, 2, 1);
دع svg = صدّر_إلى_svg(جرس, {
  xRange: { min: -5, max: 5 },
  resolution: 100
});

اطبع("طول كود SVG: " + svg.length);
```

---

## 🌟 مثال متقدم: شكل ورقة شجر

```javascript
دع ورقة = أنشئ_معادلة_شكل();

// الجزء السفلي (خط مستقيم)
ورقة.setGlobalLinear({ beta: 0.1, gamma: 0 });

// الجزء الأيسر (منحنى)
ورقة.addSigmoidTerm({
  alpha: 2,
  n: 3,
  k: 50,
  x0: 2
}, {
  lineColorStart: '#228B22',
  lineColorEnd: '#32CD32',
  lineWidthStart: 2,
  lineWidthEnd: 1
});

// الجزء الأيمن (منحنى)
ورقة.addSigmoidTerm({
  alpha: -2,
  n: 3,
  k: 50,
  x0: 8
}, {
  lineColorStart: '#32CD32',
  lineColorEnd: '#228B22',
  lineWidthStart: 1,
  lineWidthEnd: 2
});

// رسم الشكل
دع نتيجة = ارسم_معادلة(ورقة, {
  xRange: { min: 0, max: 10 },
  resolution: 100,
  enableGradients: true,
  enableLineWidthGradients: true
});
```

---

## 📖 الوصول من المكتبة القياسية

جميع الدوال متاحة من خلال المكتبة القياسية:

```javascript
// الوصول المباشر
أنشئ_خط_مستقيم(2, 3);
أنشئ_دالة_خطوة(5, 1);
أنشئ_منحنى_جرسي(0, 2, 1);
أنشئ_موجة_مربعة(4, 1, 2);
أنشئ_سيغمويد(1, 1, 1, 0);
أنشئ_معادلة_شكل();
استنبط_معادلة(بيانات);
ارسم_معادلة(معادلة, خيارات);
صدّر_إلى_svg(معادلة, خيارات);

// أو من خلال الكائن الذكاء_الرياضي
الذكاء_الرياضي.دوال_مساعدة.خط_مستقيم(2, 3);
الذكاء_الرياضي.محرك_الاستنباط.infer(بيانات);
الذكاء_الرياضي.محرك_الرسام.draw(معادلة, خيارات);
```

---

## 🎯 الفوائد الرئيسية

1. **✅ بدون شبكات عصبية** - رياضيات بحتة
2. **✅ تعلم من صورة واحدة** - لا حاجة لآلاف الصور
3. **✅ معادلات قابلة للتفسير** - تفهم ما يحدث
4. **✅ تحكم كامل في المعاملات** - غيّر القيم بسهولة
5. **✅ خصائص بصرية متقدمة** - ألوان، سمك، تدرجات
6. **✅ استنباط تلقائي** - استخرج المعادلات من البيانات
7. **✅ رسم وتصدير** - SVG، Canvas، وغيرها

---

## 📚 المراجع

- **الوثائق الكاملة**: `docs/MATHEMATICAL_INTELLIGENCE_GUIDE.md`
- **البحث الأكاديمي**: `taq.md`
- **أمثلة TypeScript**: `examples/mathematical-intelligence-demo.ts`
- **أمثلة البيان**: `examples/mathematical-intelligence.bn`
- **الاختبارات**: `tests/mathematical-intelligence.test.ts`
- **اختبارات التكامل**: `tests/mathematical-intelligence-integration.test.ts`

---

## 🚀 ابدأ الآن!

```bash
# تشغيل الأمثلة
npm run build
node dist/examples/mathematical-intelligence-demo.js

# تشغيل الاختبارات
npm test -- mathematical-intelligence
```

---

**صُنع بـ ❤️ بواسطة باسل يحيى عبدالله**  
**Made with ❤️ by Basel Yahya Abdullah**

