/**
 * 🎯 أمثلة على أسس الذكاء الرياضي في لغة البيان
 * Mathematical Intelligence Foundations Examples in Bayan Language
 * 
 * هذا الملف يوضح كيفية استخدام المعادلات المتكيفة ودالة سيغمويد المعممة
 * ومعادلة الشكل العام في لغة البيان
 */

// ═══════════════════════════════════════════════════════════════════════════
// 📊 القسم الأول: دالة سيغمويد المعممة - Generalized Sigmoid
// ═══════════════════════════════════════════════════════════════════════════

اطبع("🎯 أسس الذكاء الرياضي - Mathematical Intelligence Foundations");
اطبع("═══════════════════════════════════════════════════════════════════════════");
اطبع("");

اطبع("📊 القسم الأول: دالة سيغمويد المعممة");
اطبع("   Generalized Sigmoid: σₙ(x; k, x₀) = 1 / (1 + e^(-k(x - x₀)^n))");
اطبع("");

// مثال 1: سيغمويد تقليدية (n=1)
اطبع("🔹 مثال 1: سيغمويد تقليدية (n=1)");
دع سيغمويد_تقليدية = أنشئ_سيغمويد(1, 1, 1, 0);
اطبع("   σ₁(0) = " + سيغمويد_تقليدية.evaluate(0));  // 0.5
اطبع("   σ₁(1) = " + سيغمويد_تقليدية.evaluate(1));  // ~0.73
اطبع("   σ₁(-1) = " + سيغمويد_تقليدية.evaluate(-1)); // ~0.27
اطبع("");

// مثال 2: سيغمويد حادة (n=7)
اطبع("🔹 مثال 2: سيغمويد حادة (n=7) - للانتقالات الحادة");
دع سيغمويد_حادة = أنشئ_سيغمويد(1, 7, 100, 0);
اطبع("   σ₇(0) = " + سيغمويد_حادة.evaluate(0));    // ~0.5
اطبع("   σ₇(0.1) = " + سيغمويد_حادة.evaluate(0.1));  // ~1
اطبع("   σ₇(-0.1) = " + سيغمويد_حادة.evaluate(-0.1)); // ~0
اطبع("");

// مثال 3: منحنى جرسي (n=2)
اطبع("🔹 مثال 3: منحنى جرسي (n=2) - للتوزيعات");
دع منحنى_جرسي = أنشئ_سيغمويد(1, 2, 10, 0);
اطبع("   σ₂(0) = " + منحنى_جرسي.evaluate(0));   // ~0.5
اطبع("   σ₂(0.5) = " + منحنى_جرسي.evaluate(0.5)); // ~0.99
اطبع("   σ₂(1) = " + منحنى_جرسي.evaluate(1));   // ~1
اطبع("");

// ═══════════════════════════════════════════════════════════════════════════
// 📏 القسم الثاني: المكون الخطي - Linear Component
// ═══════════════════════════════════════════════════════════════════════════

اطبع("📏 القسم الثاني: المكون الخطي");
اطبع("   Linear Component: L(x; β, γ) = βx + γ");
اطبع("");

// مثال 1: خط مستقيم بسيط
اطبع("🔹 مثال 1: خط مستقيم بميل 2 ونقطة تقاطع 3");
دع خط = أنشئ_خط_مستقيم(2, 3);
اطبع("   L(0) = " + خط.evaluate(0));  // 3
اطبع("   L(5) = " + خط.evaluate(5));  // 13
اطبع("   L(10) = " + خط.evaluate(10)); // 23
اطبع("");

// ═══════════════════════════════════════════════════════════════════════════
// 🎨 القسم الثالث: معادلة الشكل العام - General Shape Equation
// ═══════════════════════════════════════════════════════════════════════════

اطبع("🎨 القسم الثالث: معادلة الشكل العام");
اطبع("   General Shape Equation: f̂(x) = Σᵢ [αᵢ·σₙᵢ(x; kᵢ, x₀ᵢ)] + L(x; β, γ)");
اطبع("");

// مثال 1: معادلة بسيطة (خطي فقط)
اطبع("🔹 مثال 1: معادلة خطية بسيطة");
دع معادلة_بسيطة = أنشئ_معادلة_شكل();
معادلة_بسيطة.setGlobalLinear({ beta: 0.5, gamma: 0 });
اطبع("   f(0) = " + معادلة_بسيطة.evaluate(0));   // 0
اطبع("   f(10) = " + معادلة_بسيطة.evaluate(10)); // 5
اطبع("");

// مثال 2: معادلة مركبة (خطي + سيغمويد)
اطبع("🔹 مثال 2: معادلة مركبة (خطي + سيغمويد)");
دع معادلة_مركبة = أنشئ_معادلة_شكل();
معادلة_مركبة.setGlobalLinear({ beta: 0.5, gamma: 0 });
معادلة_مركبة.addSigmoidTerm({
  alpha: 2,
  n: 1,
  k: 1,
  x0: 5
});
اطبع("   f(0) = " + معادلة_مركبة.evaluate(0));  // ~0.013
اطبع("   f(5) = " + معادلة_مركبة.evaluate(5));  // 3.5
اطبع("   f(10) = " + معادلة_مركبة.evaluate(10)); // ~7
اطبع("");

// مثال 3: شكل ورقة شجر (مثال متقدم)
اطبع("🔹 مثال 3: شكل ورقة شجر - Leaf Shape");
اطبع("   (مثال على استخدام الخصائص البصرية)");
دع شكل_ورقة = أنشئ_معادلة_شكل();

// الجزء السفلي (خط مستقيم)
شكل_ورقة.setGlobalLinear({ beta: 0.1, gamma: 0 });

// الجزء الأيسر (منحنى)
شكل_ورقة.addSigmoidTerm({
  alpha: 2,
  n: 3,
  k: 50,
  x0: 2
}, {
  lineColorStart: '#228B22',  // أخضر غامق
  lineColorEnd: '#32CD32',    // أخضر فاتح
  lineWidthStart: 2,
  lineWidthEnd: 1
});

// الجزء الأيمن (منحنى)
شكل_ورقة.addSigmoidTerm({
  alpha: -2,
  n: 3,
  k: 50,
  x0: 8
}, {
  lineColorStart: '#32CD32',  // أخضر فاتح
  lineColorEnd: '#228B22',    // أخضر غامق
  lineWidthStart: 1,
  lineWidthEnd: 2
});

اطبع("   تم إنشاء شكل ورقة شجر بخصائص بصرية متدرجة");
اطبع("   f(0) = " + شكل_ورقة.evaluate(0));
اطبع("   f(5) = " + شكل_ورقة.evaluate(5));
اطبع("   f(10) = " + شكل_ورقة.evaluate(10));
اطبع("");

// ═══════════════════════════════════════════════════════════════════════════
// 🛠️ القسم الرابع: الدوال المساعدة - Helper Functions
// ═══════════════════════════════════════════════════════════════════════════

اطبع("🛠️ القسم الرابع: الدوال المساعدة");
اطبع("");

// مثال 1: دالة خطوة
اطبع("🔹 مثال 1: دالة خطوة (Step Function)");
دع دالة_خطوة = أنشئ_دالة_خطوة(5, 1);
اطبع("   step(4) = " + دالة_خطوة.evaluate(4));  // ~0
اطبع("   step(5) = " + دالة_خطوة.evaluate(5));  // ~0.5
اطبع("   step(6) = " + دالة_خطوة.evaluate(6));  // ~1
اطبع("");

// مثال 2: منحنى جرسي
اطبع("🔹 مثال 2: منحنى جرسي (Bell Curve)");
دع جرس = أنشئ_منحنى_جرسي(0, 2, 1);
اطبع("   bell(-2) = " + جرس.evaluate(-2)); // ~0
اطبع("   bell(0) = " + جرس.evaluate(0));   // ~0.5
اطبع("   bell(2) = " + جرس.evaluate(2));   // ~1
اطبع("");

// مثال 3: موجة مربعة
اطبع("🔹 مثال 3: موجة مربعة (Square Wave)");
دع موجة = أنشئ_موجة_مربعة(4, 1, 2);
اطبع("   تم إنشاء موجة مربعة بدورة 4 وارتفاع 1 ودورتين");
اطبع("");

// ═══════════════════════════════════════════════════════════════════════════
// 👁️ القسم الخامس: محرك العين المستنبطة - Inference Engine
// ═══════════════════════════════════════════════════════════════════════════

اطبع("👁️ القسم الخامس: محرك العين المستنبطة");
اطبع("   (استنباط المعادلات من البيانات)");
اطبع("");

// مثال: استنباط خط مستقيم من نقاط
اطبع("🔹 مثال: استنباط خط مستقيم من نقاط");
دع بيانات = [
  { x: 0, y: 1 },
  { x: 1, y: 3 },
  { x: 2, y: 5 },
  { x: 3, y: 7 }
];

دع معادلة_مستنبطة = استنبط_معادلة(بيانات);
اطبع("   البيانات: (0,1), (1,3), (2,5), (3,7)");
اطبع("   المعادلة المستنبطة:");
اطبع("   f(0) = " + معادلة_مستنبطة.evaluate(0));  // ~1
اطبع("   f(1) = " + معادلة_مستنبطة.evaluate(1));  // ~3
اطبع("   f(4) = " + معادلة_مستنبطة.evaluate(4));  // ~9 (تنبؤ)
اطبع("");

// ═══════════════════════════════════════════════════════════════════════════
// 🎨 القسم السادس: محرك الرسام - Drawing Engine
// ═══════════════════════════════════════════════════════════════════════════

اطبع("🎨 القسم السادس: محرك الرسام");
اطبع("");

// مثال: رسم معادلة
اطبع("🔹 مثال: رسم معادلة خط مستقيم");
دع خط_للرسم = أنشئ_خط_مستقيم(2, 3);
دع نتيجة_الرسم = ارسم_معادلة(خط_للرسم, {
  xRange: { min: 0, max: 10 },
  resolution: 100
});
اطبع("   تم رسم الخط من x=0 إلى x=10 بدقة 100 نقطة");
اطبع("   عدد النقاط: " + نتيجة_الرسم.points.length);
اطبع("");

// مثال: تصدير إلى SVG
اطبع("🔹 مثال: تصدير معادلة إلى SVG");
دع جرس_للتصدير = أنشئ_منحنى_جرسي(0, 2, 1);
دع svg = صدّر_إلى_svg(جرس_للتصدير, {
  xRange: { min: -5, max: 5 },
  resolution: 100
});
اطبع("   تم تصدير المنحنى الجرسي إلى SVG");
اطبع("   طول كود SVG: " + svg.length + " حرف");
اطبع("");

// ═══════════════════════════════════════════════════════════════════════════
// 🌟 القسم السابع: مثال متقدم - Advanced Example
// ═══════════════════════════════════════════════════════════════════════════

اطبع("🌟 القسم السابع: مثال متقدم - دالة مركبة معقدة");
اطبع("");

// إنشاء دالة مركبة تجمع عدة أشكال
دع دالة_مركبة = أنشئ_معادلة_شكل();

// خط أساسي
دالة_مركبة.setGlobalLinear({ beta: 0.2, gamma: 1 });

// موجة أولى
دالة_مركبة.addSigmoidTerm({
  alpha: 1.5,
  n: 1,
  k: 2,
  x0: 2
}, {
  lineColorStart: '#FF0000',
  lineColorEnd: '#FF6666'
});

// موجة ثانية
دالة_مركبة.addSigmoidTerm({
  alpha: -1,
  n: 1,
  k: 2,
  x0: 5
}, {
  lineColorStart: '#0000FF',
  lineColorEnd: '#6666FF'
});

// موجة ثالثة
دالة_مركبة.addSigmoidTerm({
  alpha: 0.8,
  n: 1,
  k: 2,
  x0: 8
}, {
  lineColorStart: '#00FF00',
  lineColorEnd: '#66FF66'
});

اطبع("🔹 دالة مركبة من 3 موجات سيغمويد + خط أساسي:");
اطبع("   f(0) = " + دالة_مركبة.evaluate(0));
اطبع("   f(2) = " + دالة_مركبة.evaluate(2));
اطبع("   f(5) = " + دالة_مركبة.evaluate(5));
اطبع("   f(8) = " + دالة_مركبة.evaluate(8));
اطبع("   f(10) = " + دالة_مركبة.evaluate(10));
اطبع("");

// ═══════════════════════════════════════════════════════════════════════════
// ✅ الخلاصة - Summary
// ═══════════════════════════════════════════════════════════════════════════

اطبع("✅ الخلاصة - Summary");
اطبع("═══════════════════════════════════════════════════════════════════════════");
اطبع("");
اطبع("🎯 تم عرض جميع مكونات أسس الذكاء الرياضي:");
اطبع("   ✓ دالة سيغمويد المعممة (Generalized Sigmoid)");
اطبع("   ✓ المكون الخطي (Linear Component)");
اطبع("   ✓ معادلة الشكل العام (General Shape Equation)");
اطبع("   ✓ الدوال المساعدة (Helper Functions)");
اطبع("   ✓ محرك العين المستنبطة (Inference Engine)");
اطبع("   ✓ محرك الرسام (Drawing Engine)");
اطبع("");
اطبع("🌟 هذا النظام يمكّن المبرمج من:");
اطبع("   • إنشاء أشكال معقدة بمعادلات بسيطة");
اطبع("   • تغيير المعاملات بسهولة للحصول على أشكال مختلفة");
اطبع("   • استنباط المعادلات من البيانات تلقائياً");
اطبع("   • رسم الأشكال وتصديرها إلى SVG");
اطبع("   • التحكم في الخصائص البصرية (الألوان، السمك، التدرجات)");
اطبع("");
اطبع("💡 كل هذا بدون شبكات عصبية - رياضيات بحتة!");
اطبع("");

