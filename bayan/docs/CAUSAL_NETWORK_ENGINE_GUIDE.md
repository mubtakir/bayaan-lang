# دليل محرك الشبكات السببية - Causal Network Engine Guide

## 📋 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المفاهيم الأساسية](#المفاهيم-الأساسية)
3. [البنية الأساسية](#البنية-الأساسية)
4. [أوامر البناء](#أوامر-البناء)
5. [أنواع العلاقات](#أنواع-العلاقات)
6. [الاستدلال السببي](#الاستدلال-السببي)
7. [أمثلة عملية](#أمثلة-عملية)
8. [الاستخدام المتقدم](#الاستخدام-المتقدم)

---

## نظرة عامة

**محرك الشبكات السببية** هو جزء من **نواة لغة البيان** يوفر بنية أساسية لبناء أي نوع من الشبكات السببية.

### الهدف

تمكين المبرمجين من بناء شبكات سببية في أي مجال:
- المهن والحياة اليومية
- العلاقات الاجتماعية
- الحالات النفسية
- القوانين العلمية
- النظريات الفلسفية
- الأحداث التاريخية
- أي مجال مخصص

### المزايا

✅ **عام وقابل للتوسع** - يعمل في أي مجال  
✅ **مستقل عن المجال** - لا يفترض معرفة مسبقة  
✅ **استدلال معقد** - يدعم السلاسل السببية الطويلة  
✅ **قابل للدمج** - يمكن دمج شبكات متعددة  
✅ **قابل للتصدير** - يدعم صيغ متعددة  

---

## المفاهيم الأساسية

### 1. العقدة (Node)

العقدة هي وحدة أساسية في الشبكة تمثل:
- مفهوم (concept)
- كيان (entity)
- حالة (state)
- قانون (law)
- عاطفة (emotion)
- سلوك (behavior)
- أي شيء آخر

**البنية:**
```
node(id, type, properties)
```

**مثال:**
```bayan
add_node("my_network", "نجار", "profession", "مهنة")
```

### 2. العلاقة السببية (Causal Relation)

العلاقة السببية تربط عقدتين وتحدد كيف تؤثر إحداهما على الأخرى.

**البنية:**
```
causal_relation(from_node, to_node, relation_type, strength, conditions)
```

**مثال:**
```bayan
add_causal_relation("my_network", "نجار", "خشب", "requires", "0.9")
```

### 3. الشبكة (Network)

الشبكة هي مجموعة من العقد والعلاقات في مجال معين.

**البنية:**
```
network(id, name, domain, nodes, relations)
```

**مثال:**
```bayan
create_network("prof_net", "شبكة المهن", "professional")
```

---

## البنية الأساسية

### أنواع العقد

| النوع | الوصف | مثال |
|-------|-------|-------|
| `concept` | مفهوم عام | معرفة، حقيقة |
| `entity` | كيان محدد | نجار، طبيب |
| `state` | حالة | راحة، تعب |
| `emotion` | عاطفة | سعادة، قلق |
| `behavior` | سلوك | تعاون، خيانة |
| `property` | خاصية | كتلة، سرعة |
| `law` | قانون | قانون نيوتن |
| `tool` | أداة | منشار، مطرقة |
| `material` | مادة | خشب، حديد |
| `product` | منتج | طاولة، كرسي |

### المجالات المدعومة

| المجال | الوصف | أمثلة |
|--------|-------|--------|
| `professional` | المهن والحياة اليومية | نجار، طبيب، معلم |
| `social` | العلاقات الاجتماعية | صداقة، ثقة، تعاون |
| `psychological` | الحالات النفسية | قلق، سعادة، ضغط |
| `scientific` | القوانين العلمية | فيزياء، كيمياء، أحياء |
| `philosophical` | النظريات الفلسفية | معرفة، حقيقة، يقين |
| `historical` | الأحداث التاريخية | حرب، سلام، ثورة |
| `custom` | مجال مخصص | أي مجال آخر |

---

## أوامر البناء

### 1. إنشاء شبكة جديدة

```bayan
create_network(network_id, network_name, domain)
```

**المعاملات:**
- `network_id`: معرف فريد للشبكة
- `network_name`: اسم الشبكة
- `domain`: المجال (professional, social, psychological, etc.)

**مثال:**
```bayan
create_network("my_net", "شبكتي الأولى", "professional")
```

### 2. إضافة عقدة

```bayan
add_node(network_id, node_id, node_type, properties)
```

**المعاملات:**
- `network_id`: معرف الشبكة
- `node_id`: معرف العقدة
- `node_type`: نوع العقدة (concept, entity, state, etc.)
- `properties`: خصائص إضافية

**مثال:**
```bayan
add_node("my_net", "نجار", "profession", "مهنة")
add_node("my_net", "خشب", "material", "مادة")
```

### 3. إضافة علاقة سببية

```bayan
add_causal_relation(network_id, from_node, to_node, relation_type, strength)
```

**المعاملات:**
- `network_id`: معرف الشبكة
- `from_node`: العقدة المصدر
- `to_node`: العقدة الهدف
- `relation_type`: نوع العلاقة (causes, enables, prevents, etc.)
- `strength`: قوة العلاقة (0.0 إلى 1.0)

**مثال:**
```bayan
add_causal_relation("my_net", "نجار", "خشب", "requires", "0.9")
```

---

## أنواع العلاقات

### العلاقات السببية الأساسية

| النوع | الوصف | مثال | القوة النموذجية |
|-------|-------|-------|------------------|
| `causes` | A يسبب B | مطر → بلل | 0.9 - 1.0 |
| `enables` | A يمكّن B | مفتاح → فتح_باب | 0.8 - 0.95 |
| `prevents` | A يمنع B | قفل → دخول | 0.8 - 0.95 |
| `requires` | A يتطلب B | نجار → منشار | 0.8 - 0.95 |
| `leads_to` | A يؤدي إلى B | دراسة → نجاح | 0.7 - 0.9 |
| `results_from` | A ينتج عن B | نجاح → اجتهاد | 0.7 - 0.9 |

### العلاقات التعديلية

| النوع | الوصف | مثال | القوة النموذجية |
|-------|-------|-------|------------------|
| `enhances` | A يعزز B | رياضة → صحة | 0.7 - 0.9 |
| `weakens` | A يضعف B | مرض → قوة | 0.7 - 0.9 |

### العلاقات الارتباطية

| النوع | الوصف | مثال | القوة النموذجية |
|-------|-------|-------|------------------|
| `correlates_with` | A يتزامن مع B | دخان → نار | 0.6 - 0.9 |
| `contradicts` | A يتناقض مع B | صدق → كذب | 0.9 - 1.0 |
| `complements` | A يكمل B | ملح → فلفل | 0.5 - 0.8 |
| `depends_on` | A يعتمد على B | نبات → ماء | 0.8 - 1.0 |

---

## الاستدلال السببي

### 1. إيجاد سلسلة سببية

```bayan
infer_causal_chain(network_id, start_node, end_node, max_depth)
```

**الوصف:** يبحث عن مسار سببي من `start_node` إلى `end_node`

**مثال:**
```bayan
infer_causal_chain("my_net", "نجار", "شبع", "5")
# النتيجة: نجار → خشب → طاولة → أكل → شبع
```

### 2. إيجاد جميع التأثيرات

```bayan
find_all_effects(network_id, node_id, depth)
```

**الوصف:** يجد جميع العقد التي تتأثر بـ `node_id` حتى عمق `depth`

**مثال:**
```bayan
find_all_effects("my_net", "نجار", "3")
# النتيجة: منشار، خشب، طاولة، كرسي، ...
```

### 3. إيجاد جميع الأسباب

```bayan
find_all_causes(network_id, node_id, depth)
```

**الوصف:** يجد جميع العقد التي تسبب `node_id` حتى عمق `depth`

**مثال:**
```bayan
find_all_causes("my_net", "شبع", "3")
# النتيجة: أكل، طعام، طاولة، نجار، ...
```

### 4. التنبؤ بالنتيجة

```bayan
predict_outcome(network_id, initial_state, actions)
```

**الوصف:** يتنبأ بالنتيجة بناءً على حالة أولية وإجراءات

**مثال:**
```bayan
predict_outcome("psych_net", "قلق", "استرخاء")
# النتيجة: راحة → سعادة
```

---

## أمثلة عملية

### مثال 1: شبكة مهنية بسيطة

```bayan
hybrid {
    # إنشاء الشبكة
    create_network("carpentry", "شبكة النجارة", "professional")
    
    # إضافة العقد
    add_node("carpentry", "نجار", "profession", "مهنة")
    add_node("carpentry", "خشب", "material", "مادة")
    add_node("carpentry", "منشار", "tool", "أداة")
    add_node("carpentry", "طاولة", "product", "منتج")
    
    # إضافة العلاقات
    add_causal_relation("carpentry", "نجار", "منشار", "requires", "0.9")
    add_causal_relation("carpentry", "نجار", "خشب", "requires", "0.95")
    add_causal_relation("carpentry", "منشار", "طاولة", "enables", "0.8")
    add_causal_relation("carpentry", "خشب", "طاولة", "enables", "0.9")
    
    # الاستدلال
    infer_causal_chain("carpentry", "نجار", "طاولة", "3")
}
```

### مثال 2: شبكة نفسية

```bayan
hybrid {
    # إنشاء الشبكة
    create_network("stress", "شبكة الضغط النفسي", "psychological")
    
    # إضافة العقد
    add_node("stress", "ضغط", "state", "حالة")
    add_node("stress", "قلق", "emotion", "عاطفة")
    add_node("stress", "أرق", "symptom", "عرض")
    add_node("stress", "استرخاء", "activity", "نشاط")
    add_node("stress", "راحة", "state", "حالة")
    
    # إضافة العلاقات
    add_causal_relation("stress", "ضغط", "قلق", "causes", "0.9")
    add_causal_relation("stress", "قلق", "أرق", "causes", "0.85")
    add_causal_relation("stress", "استرخاء", "قلق", "weakens", "0.8")
    add_causal_relation("stress", "استرخاء", "راحة", "causes", "0.9")
    
    # التنبؤ
    predict_outcome("stress", "قلق", "استرخاء")
}
```

### مثال 3: شبكة علمية (فيزياء)

```bayan
hybrid {
    # إنشاء الشبكة
    create_network("newton", "قوانين نيوتن", "scientific")
    
    # إضافة العقد
    add_node("newton", "قوة", "concept", "مفهوم")
    add_node("newton", "كتلة", "property", "خاصية")
    add_node("newton", "تسارع", "concept", "مفهوم")
    add_node("newton", "حركة", "state", "حالة")
    
    # إضافة العلاقات (F = ma)
    add_causal_relation("newton", "قوة", "تسارع", "causes", "1.0")
    add_causal_relation("newton", "كتلة", "تسارع", "weakens", "1.0")
    add_causal_relation("newton", "تسارع", "حركة", "causes", "1.0")
    
    # الاستدلال
    infer_causal_chain("newton", "قوة", "حركة", "3")
}
```

---

## الاستخدام المتقدم

### 1. دمج شبكتين

```bayan
merge_networks(network1_id, network2_id, new_network_id)
```

**مثال:**
```bayan
merge_networks("carpentry", "blacksmith", "crafts")
```

### 2. استخراج شبكة فرعية

```bayan
extract_subnetwork(network_id, node_list, new_network_id)
```

**مثال:**
```bayan
extract_subnetwork("big_net", "نجار,خشب,طاولة", "small_net")
```

### 3. تحليل الشبكة

```bayan
analyze_network(network_id)
```

**النتائج:**
- عدد العقد
- عدد العلاقات
- العقد المركزية (أكثر العقد ارتباطاً)
- أطول السلاسل السببية

### 4. التصدير والاستيراد

```bayan
# تصدير
export_network("my_net", "json")
export_network("my_net", "graphml")

# استيراد
import_network("network.json", "imported_net")
```

### 5. التصور

```bayan
# تصور الشبكة كاملة
visualize_network("my_net", "hierarchical")

# تصور مسار محدد
visualize_causal_path("my_net", "نجار", "شبع")
```

---

## الخلاصة

محرك الشبكات السببية هو أداة قوية ومرنة تمكن المبرمجين من:

✅ بناء شبكات سببية في أي مجال  
✅ تمثيل المعرفة بطريقة منطقية  
✅ الاستدلال السببي المعقد  
✅ التنبؤ بالنتائج  
✅ بناء نظريات وتطبيقات ذكية  

**الاستخدام في Bayan LLM:**
- النموذج اللغوي يستخدم هذا المحرك لبناء شبكات ديناميكياً
- يمكنه الاستدلال عبر الشبكات لفهم السياق
- يمكنه بناء نظريات جديدة في مجالات مختلفة

---

**للمزيد من الأمثلة، راجع:** `bayan/examples/causal_network_examples.by`

