hybrid {
  كيان أحمد {
    "خصائص": {"س": {"نوع": "عددي", "قيمة": 0.0}},
    "أفعال": {
      "اذهب": {"تأثيرات": [{"on": "س", "formula": "value + 2*sensitivity"}]}
    }
  }

  # دالة اذهب (تستدعي perform("اذهب", ...))
  اذهب(["أحمد.0.5"])    # س += 2*0.5 = 1.0
}

# استعلام
query property("أحمد", "س", ?X).

