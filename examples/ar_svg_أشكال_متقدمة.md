# أشكال SVG متقدمة (مثلث، بيضوي، اسطوانة، كرة)

```bayan
include "gfx/svg.bayan"

عرض = 520
ارتفاع = 300
داخل = ""

داخل = داخل + مثلث(40, 220, 140, 220, 90, 120, "#ffd166", "#111", 2)
داخل = داخل + بيضوي(230, 170, 60, 35, "#06d6a0", "#111", 2)
داخل = داخل + اسطوانة(320, 80, 80, 140, "#a0c4ff", "#8ecae6", "#111", 2)
داخل = داخل + كرة(440, 160, 50, "#e5989b", "#111", 2)

داخل = داخل + نص(20, 30, "أشكال متقدمة: مثلث، بيضوي، اسطوانة، كرة", 12, "#333")
print(لف_SVG(عرض, ارتفاع, داخل))
```

